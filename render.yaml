services:
  - type: web
    name: university-portal
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend/flask_backend && gunicorn -w 2 --threads 8 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.8"
